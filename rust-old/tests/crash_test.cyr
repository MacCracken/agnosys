include "lib/string.cyr"
include "lib/fmt.cyr"
include "lib/alloc.cyr"
include "lib/vec.cyr"
include "lib/str.cyr"
include "lib/tagged.cyr"
include "lib/syscalls.cyr"
include "lib/io.cyr"
include "lib/hashmap.cyr"
include "lib/process.cyr"
include "lib/fs.cyr"
include "lib/bench.cyr"
include "src/error.cyr"
include "src/syscall.cyr"
include "src/security.cyr"

fn task_a() { var x = now_ns(); return x; }
fn task_b() { var x = now_ns(); return x; }
fn task_c() { var x = now_ns(); return x; }
fn task_d() { var x = now_ns(); return x; }
fn task_e() { var x = now_ns(); return x; }
fn task_f() { var x = now_ns(); return x; }
fn task_g() { var x = now_ns(); return x; }
fn task_h() { var x = now_ns(); return x; }
fn task_i() { var x = now_ns(); return x; }
fn task_j() { var x = now_ns(); return x; }
fn task_k() { var x = now_ns(); return x; }
fn task_l() { var x = now_ns(); return x; }
fn task_m() { var x = now_ns(); return x; }

fn main() {
    alloc_init();
    task_a(); task_b(); task_c(); task_d(); task_e();
    task_f(); task_g(); task_h(); task_i(); task_j();
    task_k(); task_l(); task_m();
    println("ok");
    return 0;
}

var r = main();
syscall(SYS_EXIT, r);
